# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
'on':
  push:
    branches:
      - main
env:
  CI: false
  NEXT_PUBLIC_APIKEY: ${{ secrets.FIREBASE_APIKEY }}
  NEXT_PUBLIC_AUTHDOMAIN: ${{ secrets.FIREBASE_AUTHDOMAIN }}
  NEXT_PUBLIC_DATABASEURL: ${{ secrets.FIREBASE_DATABASEURL }}
  NEXT_PUBLIC_PROJECTID: ${{ secrets.FIREBASE_PROJECTID }}
  NEXT_PUBLIC_STORAGEBUCKET: ${{ secrets.FIREBASE_STORAGEBUCKET }}
  NEXT_PUBLIC_MESSAGINGSENDERID: ${{ secrets.FIREBASE_MESSAGINGSENDERID }}
  NEXT_PUBLIC_APPID: ${{ secrets.FIREBASE_APPID }}
  NEXT_PUBLIC_MEASUREMENTID: ${{ secrets.FIREBASE_MEASUREMENTID }}
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: npm ci && npm run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_SCHOOL_OF_ATHENS_122422 }}'
          channelId: live
          projectId: school-of-athens-122422
        env:
          FIREBASE_CLI_EXPERIMENTS: webframeworks
